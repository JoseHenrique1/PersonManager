import { FormRoot } from "./FormRoot";
import { FormInput } from "./FormInput";
import { FormButton } from "./FormButton";
import { FormRow } from "./FormRow";
import { FormBetween } from "./FormBetween";

export const Form = {
  root: FormRoot,
  input: FormInput,
  button: FormButton,
  row: FormRow,
  between: FormBetween
}